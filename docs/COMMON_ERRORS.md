## CSV not found
Paths are resolved relative to the working directory.

## Plot fails with numeric error
The column contains non-numeric values.
Use transforms or recoding explicitly.

## No plots generated
Ensure plots are defined under `artifacts.plots`.
